import styled from "styled-components";

export const Container = styled.div`
  text-align: right;

  button {
    padding: 15px 35px;
    color: var(--text-link);
    font-size: 1.125rem;
    border: none;
    border-radius: 10px;
    background: var(--color-principal);
  }


`